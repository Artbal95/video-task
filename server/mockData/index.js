const videoData = require("./video.data.json")
const frameData = require("./frames.data.json")
const saveFramesData = require("./saveFrames.data.json")

module.exports = {
    videoData,
    frameData,
    saveFramesData
}
